name=busybox
version=1.36.1
source=https://busybox.net/downloads/busybox-1.36.1.tar.bz2
build() {
    cd "$WAV_SRC"
    make defconfig
    make -j"$WAV_JOBS"
}
install() {
    cd "$WAV_SRC"
    make CONFIG_PREFIX="$WAV_SYSROOT" install
}