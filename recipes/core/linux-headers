name=linux-headers
version=6.12
source=https://cdn.kernel.org/pub/linux/kernel/v6.x/linux-6.12.tar.xz
build() {
    cd "$WAV_SRC"
    make mrproper
}
install() {
    cd "$WAV_SRC"
    make headers_install INSTALL_HDR_PATH="$WAV_SYSROOT/usr"
}